name: text
description: Russian text editing and English→Russian translation
provider: mm
# ru-editor, en-ru-translator-adv, and krrkt are baked into the image by
# seed-plugins.sh (copied from miolamio/agent-skills), so no runtime install
# is needed. Add more via the marketplace if you need ru-textovod or
# ascii-art-beautifier.
plugins: []
settings:
  effortLevel: high
